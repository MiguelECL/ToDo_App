import { Typography } from "@mui/material";

const NavBar = () => {
    return (  
        <Typography variant="h4" sx={{paddingBottom: 2, paddingTop: 2, justifySelf: "center"}}>To-Dooo (Revamped)</Typography>
    );
}
 
export default NavBar;